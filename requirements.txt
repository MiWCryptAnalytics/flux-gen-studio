# FLUX Gen Studio — verified working set (Python 3.14, CUDA 13, RTX 3090)
#
# NOTE ON torch: the venv is created with --system-site-packages so it reuses
# the system CUDA build of torch (2.12.1) instead of downloading its own.
# PyQt5 and Pillow also come from the system. If your system lacks them,
# uncomment the pins below.

diffusers==0.39.0
transformers==5.14.1
accelerate
sentencepiece
protobuf

# torch==2.12.1
# PyQt5==5.15.11
# Pillow
